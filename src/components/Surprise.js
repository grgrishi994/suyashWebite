// src/components/Surprise.js

import React from 'react';
import './Surprise.css';

const Surprise = () => {
  return (
    <div className="surprise-section">
      {/* <h2 className="surprise-heading">Surprise!</h2> */}
      <div className="surprise-card">
        <p className="surprise-message">
          🎉 I hope you liked the surprise, baby! 🎉 But this is just the 1st one; many more gifts are waiting for you! 🎁
        </p>
        {/* Add more content or images as needed */}
      </div>
    </div>
  );
};

export default Surprise;
